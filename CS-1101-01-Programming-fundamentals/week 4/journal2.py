# The function accepts two arguments to calculate
# the value of a right-angle triangle's hypotenuse
def hypotenuse(a,b):
    # Calculating a**2+b**2 for this step.
    result = a**2+b**2
    print(result)
    # For now it returns 0
    return 0    

# A function call with parameters.
hypotenuse(2,2)
