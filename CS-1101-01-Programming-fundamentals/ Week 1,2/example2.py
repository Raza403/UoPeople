def repeatName(name): # name is parameter.
    print(name)
    print(name)

repeatName("Ahmed") # Argument using a value.
lastName = "Raza"
repeatName(lastName) # Argument using a variable.
repeatName("Ahmed " + lastName) # Argument using an expression.


# Output below in the terminal