def nameLength(fullName): # function with a unique parameter name.
    print(len(fullName))

nameLength("Ahmed Raza")
print(fullName) # Using the parameter name outside the function.

# Output below in the terminal