def repeatName(first, last): # function with 2 parameters.
    fullName = first + " " + last # A local variable, available inside the function only.
    print(fullName)

print(fullName)

# Output below in the terminal