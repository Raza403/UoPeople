import math
def nameLength(fullName):
    name = len(fullName)#Variable name inside a function, this should be 10
    print(f"The name is {name}")

name = "Ahmed Raza" #variable name outside a function. This should be Ahmed Raza
nameLength(name)
print(name)
print(type(nameLength))
# Output below in the terminal
print(math.pi)

True = 23